@model Vidly.ViewModels.RandomMovieViewModel
@{
    ViewBag.Title = "index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


<h2>Movies</h2>

@if (Model.Movies.Count == 0)
{
    <text>There's no customers</text>
}
else
{
    <table class="table table-bordered table-hover">
        <thead>
            <tr>
                <td><strong>Movies</strong></td>
            </tr>
        </thead>
        <tbody>
            @foreach (var movie in Model.Movies)
            {
                <tr>
                    <td>@Html.ActionLink(@movie.Name, "Details", new { id = 0 }, null) </td>
                </tr>
            }
        </tbody>
    </table>

}

@*@Html.ActionLink("Olvido su contrasena", "recuperar-contrasena", new { id = 5}, null)*@





